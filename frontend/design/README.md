# Design References

Use this folder for design references that are not runtime source files.

- `figma-screens/`: screen-level exports or annotated references.
- `reference/`: product references, visual notes, or comparison screenshots.

Runtime assets should eventually live in `frontend/src/assets/` or `frontend/public/`.

