# 2026-06-27 Dev Log

## Claim: backend-implementation-handoff-summary
- Owner: JY-side Codex summary
- Started: 2026-06-27 12:55 +08:00
- Status: complete
- Goal: record the backend/API work and changed files for teammate coordination.
- Scope:
  - `jy_notes/dev_logs/2026-06-27.md`
- Avoid:
  - runtime code changes
  - secrets
  - environment files
- Expected output:
  - concise handoff summary of backend implementation, verification, and remaining risks
- Current notes:
  - this log summarizes work visible from the parent thread for JY coordination
  - Workato runtime path was intentionally skipped in favor of direct Google ecosystem APIs
- Verification plan:
  - confirm this log file exists and contains changed-file summary
- Parallel-safe work:
  - frontend can consume backend API contracts
  - Supabase schema/RLS work can continue separately

## Changes
- `requirements.txt`: added Python backend dependencies for FastAPI, Azure/OpenAI-compatible model calls, Supabase, Exa, Google Calendar/Gmail, document parsing, auth/security, and tests.
- `.gitignore`: added local/runtime ignores such as `.env`, `secrets/`, `.venv/`, `.local-data/`, Python bytecode, and pytest cache.
- `supwork_backend/`: added a FastAPI backend scaffold with:
  - demo auth/session flow for HR and interviewee accounts
  - role-shaped candidate and recruiter workflow views
  - seeded demo workflow data
  - approval requests and approval enforcement
  - audit events and candidate receipts
  - Exa research wrapper with fixture fallback
  - Azure/OpenAI-compatible model wrapper
  - direct Google Calendar/Meet scheduling client
  - direct Gmail draft/send client
  - post-interview addendum lifecycle
  - candidate-safe feedback draft generation
  - provider status endpoint showing Workato disabled
- `scripts/supwork_workflow_smoke.py`: added a simple backend smoke entry point.
- `tests/test_backend_workflow.py`: added tests for candidate visibility shaping, approval-required scheduling, fixture Google event creation, and Gmail safety blocking.
- `tests/azure_openai_key_smoke.py`: added a manual live smoke script for Azure OpenAI key/deployment checks. It loads `.env`, avoids printing the API key, and is intentionally not named `test_*.py`.
- `nic_notes/dev_logs/2026-06-27.md`: updated with backend scope claims, verification, decisions, and handoff notes.

## Verification
- `.venv\Scripts\python.exe -m unittest discover -s tests -v`: passed 4 backend workflow tests in the parent-thread verification.
- `.venv\Scripts\python.exe -m supwork_backend.main --smoke`: passed in the parent-thread verification.
- `Invoke-RestMethod http://127.0.0.1:8787/api/health`: returned `{"status":"ok","service":"supwork-backend"}` in the parent-thread verification.
- `Invoke-RestMethod http://127.0.0.1:8787/api/provider-status`: returned mock/fixture Google, Gmail, Exa, Supabase storage/auth, disabled Workato, and Azure OpenAI deployment `gpt-4.1-mini`.
- `.venv\Scripts\python.exe tests\azure_openai_key_smoke.py`: attempted once and failed with `Azure OpenAI connection failed: Connection error.` This likely needs live network access and/or endpoint/key validation.

## Decisions
- Use direct Google Calendar/Gmail APIs instead of Workato runtime integration for this build path.
- Keep fixture/mock behavior as the default demo-safe path.
- Use `.env` and `secrets/` locally, but never commit secrets.
- Accept `.local` demo login identities as strings because strict email validation rejects special-use domains.
- Keep Azure OpenAI API-key smoke script manual, not part of normal unit discovery, to avoid accidental live calls.

## Handoff / Next
- Backend URL used in verification: `http://127.0.0.1:8787`.
- Run backend locally with:
  - `.venv\Scripts\python.exe -m uvicorn supwork_backend.main:app --host 127.0.0.1 --port 8787`
- For live Google calls, provide Google OAuth refresh-token settings and `GMAIL_SENDER_EMAIL`.
- For live Azure OpenAI, rotate any previously pasted key, then set `AZURE_OPENAI_ENDPOINT`, `AZURE_OPENAI_API_KEY`, and `AZURE_OPENAI_DEPLOYMENT` in `.env`.
- Remaining risk: Supabase schema/RLS persistence is not fully implemented; current backend is fixture-backed with provider readiness detection.
- Remaining risk: live Azure OpenAI smoke has not passed yet due to connection failure.

## Claim: fake-e2e-workflow-tests
- Owner: JY-side Codex
- Started: 2026-06-27 13:05 +08:00
- Status: complete
- Goal: add fake-provider end-to-end workflow coverage for the sup'work golden path.
- Scope:
  - `tests/test_end_to_end_workflow.py`
  - `jy_notes/dev_logs/2026-06-27.md`
- Avoid:
  - `nic_notes/dev_logs/`
  - `.env`
  - `secrets/`
  - live provider calls
- Expected output:
  - local unit test that walks HR and candidate through the fake end-to-end workflow
- Current notes:
  - use `SUPWORK_PROVIDER=mock` and FastAPI `TestClient`
  - this should not touch Azure, Exa, Google, Gmail, or Supabase live services
- Verification plan:
  - `.venv\Scripts\python.exe -m unittest discover -s tests -v`
- Parallel-safe work:
  - frontend and live-provider work can continue independently

## Changes
- `tests/test_end_to_end_workflow.py`: added fake-provider golden path coverage using FastAPI `TestClient`.
- The test covers HR and candidate login, candidate/recruiter role-shaped views, fixture Exa research, interview question generation, approval-gated fixture Google Meet scheduling, candidate schedule visibility, recruiter notes, candidate addendum submission, HR acknowledgement, candidate-safe feedback draft generation, approval-gated fixture Gmail draft creation, candidate final visibility, and audit event assertions.

## Verification
- `.venv\Scripts\python.exe -m unittest discover -s tests -v`: passed 5 tests.
- The manual Azure smoke script was not run by discovery because it is intentionally named `azure_openai_key_smoke.py`.

## Decisions
- Kept E2E test fake/local only with `SUPWORK_PROVIDER=mock`; it does not call Azure, Exa, Google, Gmail, Supabase, or Workato live services.
- Used a separate Gmail approval after scheduling because scheduling approval is marked executed after the calendar action.

## Handoff / Next
- The fake golden path is now covered in one local test.
- Remaining live-demo gap: direct provider smoke tests still need live credentials/network and should stay separate from unit discovery.
- Released: `tests/test_end_to_end_workflow.py`
