# 2026-06-27 Dev Log

## Claim: standalone-system-design
- Owner: Codex
- Status: active
- Scope: `system_design/`, removal of empty `documentation/`, this dev log
- Started: 2026-06-27 10:49 +08:00
- Released:
- Will not touch: runtime code, `hackathon.md`, `AGENTS.md`, existing `jy_notes/` or `nic_notes/` plan files
- Verification plan: read generated files, check stale references, confirm `documentation/` removal, run `git diff --stat`
- Notes: Consolidating JY and Nicholas planning into a standalone sup'work hackathon system design. Product direction is seeker-first with a recruiter-side live hosted demo.

## Changes
- `system_design/*.md`: created split standalone sup'work system design from the JY and Nicholas notes plus `hackathon.md`.
- `system_design/*.md`: strengthened humane/transparency model with a candidate transparency contract, evidence-card pattern, feedback boundaries, transparency QA checklist, and correction/add-on rights.
- `system_design/*.md`: replaced demo-only view-switch framing with authenticated HR and interviewee accounts for a two-laptop hosted demo.
- `system_design/*.md`: added post-interview addendum as a first-class workflow, UX, data/API, safety, demo, and roadmap feature.
- `documentation/`: removed empty folder after migrating the system design package.

## Verification
- `Select-String` checked humane/transparency sections and stale product-name references. Only intentional copy examples remained.
- `git diff --stat` checked tracked changes; new docs are currently untracked.
- `rg` checked stale view-switch/product-name references and addendum/auth coverage.
- `Test-Path documentation` returned `False`, confirming the old folder is removed.


## Claim: supwork-brand-and-a2a-doc-cleanup
- Owner: Codex
- Status: released
- Scope: `AGENTS.md`, markdown planning docs in `system_design/`, `nic_notes/`, and `jy_notes/`
- Started: 2026-06-27 11:05 +08:00
- Released: 2026-06-27 11:14 +08:00
- Will not touch: runtime code, package/module names, existing implementation identifiers unless they are only examples
- Verification plan: scan markdown docs for stale previous product-brand strings and old lowercase planned identifiers
- Notes: Visible product branding is now `sup'work`. Old `TalentFlow IQ` references should remain only when describing the source repo/codebase being adapted.

## Changes
- `AGENTS.md`: changed product direction and coordination copy to `sup'work`, kept Google Meet as the primary live meeting path, and updated startup reading order to `system_design/README.md` plus relevant split design docs and notes.
- `system_design/*.md`, `nic_notes/*.md`, and `jy_notes/*.md`: replaced visible previous product-brand references with `sup'work`.
- `nic_notes/01-backend-integrations-plan.md` and `nic_notes/03-migration-delivery-plan.md`: changed planned example request IDs/script names from the previous prefix to `supwork_*`.

## Verification
- Case-sensitive markdown scan found no remaining previous product-brand references or old lowercase planned identifiers.
- `git status --short` still shows the intentional deletion of `AGENT_TO_AGENT.md`, modified JY notes, and untracked `AGENTS.md`, `system_design/`, and `nic_notes/`.

## Claim: supabase-system-design-update
- Owner: Codex
- Started: 2026-06-27 11:42 +08:00
- Status: complete
- Goal: document Supabase Postgres as the preferred hosted database/auth/storage stack for sup'work.
- Scope:
  - `system_design/01-product-strategy.md`
  - `system_design/04-system-architecture.md`
  - `system_design/05-integrations.md`
  - `system_design/06-data-and-api.md`
  - `system_design/08-hosting-demo-delivery.md`
  - `system_design/09-implementation-roadmap.md`
- Avoid:
  - runtime code
  - package dependency files
  - Workato recipe implementation
- Expected output:
  - system design files consistently recommend Supabase Postgres with backend-controlled access
- Current notes:
  - backend remains the control plane; frontend must not bypass approval, visibility, or audit rules through direct data access
- Verification plan:
  - scan docs for database/hosting references and run `git diff --stat`
- Parallel-safe work:
  - runtime implementation and frontend UI work can continue outside `system_design/*`

## Changes
- `system_design/01-product-strategy.md`: changed hosted database nice-to-have to Supabase Postgres with Auth and Storage.
- `system_design/04-system-architecture.md`: documented Supabase as the hosted data platform, with backend-controlled access, service-role boundaries, Storage, optional Realtime, and `jsonb` usage.
- `system_design/05-integrations.md`: added Supabase database/auth/storage to provider status and backend environment variables.
- `system_design/06-data-and-api.md`: added persistence stack guidance, Supabase Storage metadata, and draft version history for generated/edited content.
- `system_design/08-hosting-demo-delivery.md`: updated hosting shape, environment setup, preflight, and failure handling for Supabase.
- `system_design/09-implementation-roadmap.md`: added Supabase schema/seeding/storage/status work to phases, tests, acceptance criteria, risks, and open decisions.

## Verification
- `rg -n "SQLite for hackathon demo|Postgres if time allows|Hosted Postgres|Neon" system_design`: no matches.
- `git diff --stat`: confirmed scoped markdown/dev-log changes only.
- `git status --short`: confirmed modified design/dev-log files plus pre-existing untracked `.gitignore`.

## Handoff / Next
- Supabase is now the recommended hosted DB/Auth/Storage stack in the system design.
- Runtime implementation still needs schema migrations/seeds and backend provider status checks.
- Released: `system_design/*`

## Claim: backend-python-requirements
- Owner: Codex
- Started: 2026-06-27 12:05 +08:00
- Status: complete
- Goal: add a Python `requirements.txt` aligned with the Supabase, Azure OpenAI, Exa, Google, Workato, and backend API system design.
- Scope:
  - `requirements.txt`
  - `nic_notes/dev_logs/2026-06-27.md`
- Avoid:
  - runtime implementation files
  - frontend dependency files
  - secrets and environment files
- Expected output:
  - grouped dependency list for the planned Python backend/demo integration stack
- Current notes:
  - Azure OpenAI credentials must remain environment-only and should not be committed
  - use the standard OpenAI Python client against the Azure `/openai/v1` endpoint
- Verification plan:
  - read generated dependency file
  - run `git diff --stat`
- Parallel-safe work:
  - backend implementation can proceed after reviewing/pinning dependencies as needed

## Changes
- `requirements.txt`: added grouped Python backend dependencies for FastAPI, Azure OpenAI/OpenAI-compatible model calls, Supabase, Exa, Google Calendar/Gmail, document extraction, auth/security, and tests.

## Verification
- `Get-Content -Raw requirements.txt`: reviewed generated dependency file.
- `git diff --stat`: checked tracked changes; `requirements.txt` is new and currently untracked.
- `git status --short`: confirmed `requirements.txt` is present alongside existing modified design/dev-log files.

## Handoff / Next
- Rotate the pasted Azure OpenAI API key before using it in any deployed environment.
- Keep Azure/OpenAI, Supabase, Exa, Google, and Workato credentials in backend environment variables only.
- Released: `requirements.txt`

## Claim: backend-fastapi-golden-path
- Owner: Codex
- Started: 2026-06-27 12:20 +08:00
- Status: complete
- Goal: implement the Python backend control plane for the sup'work golden path with direct Google ecosystem APIs and no Workato runtime path.
- Scope:
  - `supwork_backend/`
  - `tests/`
  - `scripts/`
  - `requirements.txt`
  - `.gitignore`
  - `nic_notes/dev_logs/2026-06-27.md`
- Avoid:
  - `.env`
  - `secrets/`
  - frontend files
  - `system_design/*`
  - Workato implementation files
- Expected output:
  - FastAPI app with auth, role-shaped workflow views, evidence/research/interview/addendum/feedback endpoints, approval-gated Google Calendar/Gmail clients, provider status, and audit events
- Current notes:
  - fixture/local repository is the reliable default; Supabase configuration is detected and reported for hosted integration
  - direct Google Calendar/Gmail API path replaces Workato for scheduling and follow-up draft/send actions
  - root `.venv` should be used for verification commands
- Verification plan:
  - `.venv\Scripts\python.exe -m unittest discover -s tests -v`
  - `.venv\Scripts\python.exe -m supwork_backend.main --smoke`
- Parallel-safe work:
  - frontend can consume the API contracts once paths are in place
  - Supabase schema hardening can proceed separately from the fixture adapter

## Changes
- `.gitignore`: added local backend runtime/test ignores for `.local-data/`, caches, and Python bytecode.
- `supwork_backend/config.py`: added `.env`-aware settings, Azure/OpenAI, Supabase, Exa, Google, Gmail, and secret-file discovery.
- `supwork_backend/seed.py`, `supwork_backend/store.py`: added seeded sup'work demo state, demo sessions, role-shaped workflow views, approvals, audit events, addenda, scheduling receipts, notes, and draft tracking.
- `supwork_backend/main.py`: added FastAPI app with auth, candidate/recruiter workflow APIs, research endpoints, question generation, approval APIs, direct Google Calendar/Gmail integration endpoints, addendum lifecycle, audit log, provider status, and smoke command.
- `supwork_backend/google_clients.py`: added direct Google Calendar/Meet and Gmail draft/send clients with fixture fallback unless live Google env is configured.
- `supwork_backend/exa_client.py`: added Exa-backed research wrapper with cached fixture fallback.
- `supwork_backend/model_provider.py`: added mock-first model provider with Azure/OpenAI-compatible chat support.
- `supwork_backend/safety.py`: added candidate-facing safety checks for secret/internal-note/prohibited language leakage.
- `tests/test_backend_workflow.py`: covered candidate visibility shaping, approval-required scheduling, fixture Google event creation, and Gmail safety blocking.
- `scripts/supwork_workflow_smoke.py`: added direct smoke entry point.

## Verification
- `.venv\Scripts\python.exe -m unittest discover -s tests -v`: passed 4 tests.
- `.venv\Scripts\python.exe -m supwork_backend.main --smoke`: passed.
- `Invoke-RestMethod http://127.0.0.1:8787/api/health`: returned `{"status":"ok","service":"supwork-backend"}`.
- `Invoke-RestMethod http://127.0.0.1:8787/api/provider-status`: returned mock/fixture Google, Gmail, Exa, Supabase storage/auth, disabled Workato, and Azure OpenAI model deployment `gpt-4.1-mini`.
- Note: FastAPI/Starlette emitted a deprecation warning asking for `httpx2`; tests still pass with the current root `.venv`.

## Decisions
- Workato runtime path is intentionally disabled in provider status and not implemented; approved Google Calendar/Gmail endpoints call the Google ecosystem directly.
- Fixture mode remains the default so the demo backend works without live credentials; `SUPWORK_PROVIDER=live` plus Google OAuth refresh-token env enables live Google calls.
- `.local` demo login identities are accepted as strings because strict email validation rejects the documented demo domains.

## Handoff / Next
- Backend can be run with `.venv\Scripts\python.exe -m uvicorn supwork_backend.main:app --host 127.0.0.1 --port 8787`.
- For live Google calls, provide `GOOGLE_REFRESH_TOKEN`, Google OAuth client config in env or `secrets/client_secret*.json`, and `GMAIL_SENDER_EMAIL` for Gmail.
- For live Azure OpenAI chat, set `SUPWORK_PROVIDER=live`, `AZURE_OPENAI_ENDPOINT`, `AZURE_OPENAI_API_KEY`, and `AZURE_OPENAI_DEPLOYMENT`.
- Released: `supwork_backend/`, `tests/`, `scripts/`, `.gitignore`, `requirements.txt`

## Claim: frontend-folder-scaffold
- Owner: Codex
- Started: 2026-06-27 12:34 +08:00
- Status: complete
- Goal: create an empty-but-structured frontend workspace for future React/Figma import work.
- Scope:
  - `frontend/`
  - `nic_notes/dev_logs/2026-06-27.md`
- Avoid:
  - backend runtime code
  - `system_design/`
  - package manager files unless explicitly needed
- Expected output:
  - frontend folder tree with import drop zones, source structure, and README guidance
- Current notes:
  - Recent pull fast-forwarded to `dc32053 backend first draft`, adding `supwork_backend/`, `scripts/`, `tests/`, `backend_notes/`, `requirements.txt`, and `.gitignore`.
  - User will import the zip file and Figma images manually.
- Verification plan:
  - `rg --files frontend`
  - `git status --short`
- Parallel-safe work:
  - backend agents can continue in `supwork_backend/`, `tests/`, and `scripts/`.

## Changes
- `frontend/README.md`: added frontend workspace guidance, recent backend context, import drop zones, intended stack, and future API/type layout.
- `frontend/.gitignore`: added frontend build/env/cache ignores.
- `frontend/imports/`: added staging folders for source zip, Figma images, and Figma exports.
- `frontend/design/`: added non-runtime design reference folders.
- `frontend/src/`: added planned React source folders for app, API, assets, components, data, hooks, lib, pages, styles, and types.
- `frontend/tests/`, `frontend/e2e/`, `frontend/public/`: added placeholders for future test/static asset setup.

## Verification
- `rg --files frontend`: showed the visible README/import/design files.
- `rg --files --hidden frontend`: confirmed placeholder files across the full scaffold.
- `Get-ChildItem -Recurse -Force frontend`: confirmed hidden `.gitkeep` files and import drop-zone directories.
- `git status --short`: shows `frontend/` untracked and this dev log modified.

## Handoff / Next
- Import the prototype zip into `frontend/imports/source-zip/`.
- Import Figma images into `frontend/imports/figma-images/`.
- After import, decide whether the zip's package root should become `frontend/` directly or be merged into the prepared `frontend/src/` layout.
- Released: `frontend/`

## Claim: frontend-zip-triage
- Owner: Codex
- Started: 2026-06-27 12:43 +08:00
- Status: active
- Goal: inspect, extract, and decide how to use the two imported frontend zip files.
- Scope:
  - `frontend/`
  - `nic_notes/dev_logs/2026-06-27.md`
- Avoid:
  - backend runtime code
  - destructive cleanup of imported zip contents without explicit user confirmation
- Expected output:
  - extracted zip staging folders
  - recommendation on which frontend source to use as base
  - any safe file moves needed to prepare the frontend workspace
- Current notes:
  - Found `frontend/banani-ui-export (1).zip` and `frontend/Hackathon-Polish.zip`.
- Verification plan:
  - inspect zip entries before extraction
  - `rg --files frontend`
  - `git status --short`
- Parallel-safe work:
  - backend agents can continue independently.

## Claim: frontend-demo-polish
- Owner: Codex
- Started: 2026-06-27 12:38 +08:00
- Status: ready-for-review
- Goal: polish the promoted Vite React prototype into a credible two-account sup'work demo aligned with transparency, addendum, approval, and audit flows.
- Scope:
  - `frontend/src/`
  - `frontend/public/`
  - `frontend/package.json`
  - `frontend/vite.config.ts`
  - `frontend/tsconfig.json`
  - `frontend/components.json`
  - `frontend/README.md`
  - `nic_notes/dev_logs/2026-06-27.md`
- Avoid:
  - backend runtime code in `supwork_backend/`
  - root package/dependency files
  - `system_design/*`
  - imported raw zip/reference assets except for read-only inspection
- Expected output:
  - role-aware login and landing surfaces for interviewee and HR demo accounts
  - polished candidate workflow pages for evidence, role brief, schedule, addendum, feedback, and transparency
  - polished recruiter workflow pages for pipeline, packet, research, interview plan, approvals, addendum review, and audit
  - backend-aware but fixture-safe frontend data/API structure
- Current notes:
  - frontend remains untracked as a promoted import; preserve demo fallback behavior if backend is unavailable
  - frontend must not call Exa, Workato, Google, Gmail, or model providers directly
  - use humane copy such as `evidence suggests`, `proof to strengthen`, `candidate-visible`, and `draft for approval`
- Verification plan:
  - inspect app structure before edits
  - run `npm run build` from `frontend` when practical
  - update this dev log with changed files, verification, risks, and remaining work
- Parallel-safe work:
  - backend agents can continue in `supwork_backend/`, `tests/`, and `scripts/`
  - documentation agents can edit outside `frontend/` after coordinating on this dev log

## Changes
- `frontend/src/data/mockWorkflow.ts`: replaced imported placeholder workflow with Maya Tan / Example AI golden-path fixture, candidate-visible evidence, Exa-style sources, scheduling approval, and audit events.
- `frontend/src/context/AuthContext.tsx`: aligned demo users with `interviewee@demo.supwork.local` and `hr@demo.supwork.local`, persisted demo sessions in local storage, and kept logout explicit.
- `frontend/src/context/WorkflowContext.tsx`: aligned shared mock actions for scheduling approval, addendum submission/acknowledgement, research refresh, follow-up approval, timeline receipts, and audit events.
- `frontend/src/pages/LoginPage.tsx`: rebuilt role-aware two-account login with shared workflow preview and exact demo credentials.
- `frontend/src/components/layout/*`: added shell-level role routing protection, fixed mobile sidebar rendering, and clarified fixture-safe provider status.
- `frontend/src/pages/interviewee/*`: polished candidate home, CV evidence, role brief, prep, schedule, addendum, and feedback around evidence, source labels, approvals, addendum agency, and candidate-safe copy.
- `frontend/src/pages/hr/*`: polished pipeline, candidate packet, research, interview plan, scheduling approval, addendum review, follow-up, and audit copy for source-backed context, approval gates, visibility boundaries, and receipts.
- `frontend/src/components/research/SourceCard.tsx`: added candidate-safe/public-source labels to research cards.
- `frontend/package.json`, `frontend/package-lock.json`: corrected imported Vite dependency to current registry-compatible Vite 8/plugin-react 6 and added `typescript` for the existing typecheck script.
- `frontend/.gitignore`: ignored `.tsbuildinfo`.

## Verification
- `npm install` from `frontend`: passed after correcting Vite/plugin versions; 0 vulnerabilities.
- `npm run typecheck` from `frontend`: passed.
- `npm run build` from `frontend`: passed; Vite reported a non-blocking chunk-size warning for the single bundled app chunk.
- `npm run dev -- --host 127.0.0.1` from `frontend`: Vite started successfully in foreground and served the app; the command was stopped by the tool timeout.
- `Invoke-WebRequest http://127.0.0.1:5173`: returned `200` immediately after background start attempts, but the hidden background server did not stay alive in this environment.
- `rg` stale-name/encoding scan over `frontend/src`: no remaining old persona names or import mojibake matches.
- Browser smoke: not run because the in-app browser surface listed no available browser instances in this session.

## Decisions
- Kept frontend fixture-safe and backend-aware rather than adding direct provider calls; Exa/Google/Gmail/automation remain represented as backend-mediated receipts/actions.
- Preserved the existing route/page structure and component library while replacing stale demo data and weak copy.
- Added frontend dependency fixes inside `frontend/` only so build/typecheck can run locally.

## Handoff / Next
- Status: ready-for-review.
- Local URL when running manually: `http://127.0.0.1:5173` from `cd frontend; npm run dev`.
- Remaining work: wire `frontend/src/context/WorkflowContext.tsx` to backend view-model APIs when the hosted backend contract is stable.
- Risk: initial bundle is over 500 kB after minification; acceptable for hackathon prototype, but code-splitting can be added later.
- Risk: hidden background Vite launch exits in this Codex environment; foreground `npm run dev` works.
- Released scope: `frontend/`, `nic_notes/dev_logs/2026-06-27.md`

## Claim: live-demo-agent-fields
- Owner: Codex
- Started: 2026-06-27 13:10 +08:00
- Status: complete
- Goal: recover TalentFlow IQ-style agent-filled/live workflow behavior in sup'work so HR and interviewee screens share backend state during the demo.
- Scope:
  - `supwork_backend/`
  - `tests/`
  - `frontend/src/api/`
  - `frontend/src/context/`
  - `frontend/src/types/`
  - `frontend/src/data/`
  - `nic_notes/dev_logs/2026-06-27.md`
- Avoid:
  - broad visual redesign of page components
  - direct frontend calls to Exa, Google, Gmail, Workato, or model providers
  - Microsoft Teams / Graph as primary meeting path
  - unrelated `system_design/*` edits
- Expected output:
  - backend endpoints for demo reset, evidence analysis, round completion, feedback release, and agent trace
  - frontend API client and auth/workflow contexts that use backend state with fixture fallback
  - HR and interviewee actions that can propagate through a running backend for two-laptop roleplay
- Current notes:
  - Current frontend actions are local `setTimeout` mutations and cannot sync across laptops.
  - Backend already has auth, recruiter/candidate workflow views, Exa research, approvals, Google Calendar/Gmail fixture/live paths, addendum, notes, feedback draft, and audit events.
  - Restore useful TalentFlow IQ concepts as sup'work contracts: provider mode, source/provenance, validation state, trace IDs, and human approval boundaries.
- Verification plan:
  - backend unit tests or smoke for new endpoints
  - frontend `npm run typecheck`
  - frontend `npm run build`
  - targeted scans for stale local-only actions where practical
- Parallel-safe work:
  - visual-only frontend polish outside context/API files can proceed after coordinating on shared type changes
  - docs-only work outside this claim remains safe

## Changes
- `supwork_backend/schemas.py`: added request models for round completion and feedback release approval.
- `supwork_backend/seed.py`: added `agentRuns` seed storage for traceable demo runs.
- `supwork_backend/store.py`: added demo reset, backend-generated agent-filled evidence fields, agent-run trace storage, recruiter schedule view, round completion, draft approval linking, candidate-visible approved feedback body, and follow-up stage updates.
- `supwork_backend/main.py`: added `/api/demo/reset`, `/api/recruiter/workflows/{workflow_id}/analyze-evidence`, `/api/recruiter/workflows/{workflow_id}/rounds/{round_id}/complete`, `/api/recruiter/workflows/{workflow_id}/feedback-draft`, `/api/recruiter/workflows/{workflow_id}/feedback-release-approval`, and `/api/workflows/{workflow_id}/agent-trace`.
- `frontend/src/api/supworkClient.ts`: added backend client for auth, workflow views, evidence analysis, research, interview-plan generation, approvals, Google Meet scheduling, round completion, addenda, feedback approval, Gmail draft, trace, and reset.
- `frontend/src/context/AuthContext.tsx`: switched login to backend auth with offline demo fallback.
- `frontend/src/context/WorkflowContext.tsx`: replaced local-only mutation model with backend-backed refetch/polling adapters while preserving fixture fallback.
- `frontend/src/types/index.ts`: expanded workflow stages and added auth/session, sync, and interview-plan types.
- `frontend/src/pages/hr/InterviewPlan.tsx`: wired Generate from backend to evidence analysis + interview-plan generation and renders backend questions when present.
- `frontend/src/pages/hr/Scheduling.tsx`: wired backend scheduling approval and added Mark Complete to open the addendum window.
- `frontend/src/pages/interviewee/PostInterviewAddendum.tsx`: gated addendum submission on interview completion instead of schedule-only state.
- `frontend/src/pages/interviewee/Feedback.tsx`: recognizes backend Gmail draft/send audit events as released follow-up.
- `tests/test_backend_workflow.py`: covered agent-filled evidence analysis and trace output.
- `tests/test_end_to_end_workflow.py`: golden path now marks the interview round complete before addendum submission.

## Verification
- `python -m unittest discover -s tests -v`: passed 6 tests.
- `python -m supwork_backend.main --smoke`: passed.
- `npm run typecheck` from `frontend`: passed.
- `npm run build` from `frontend`: passed; Vite still reports the existing non-blocking chunk-size warning for the single app bundle.
- `python -m uvicorn supwork_backend.main:app --host 127.0.0.1 --port 8787`: foreground startup verified; tool timeout stopped the foreground server.
- `npm run dev` from `frontend`: foreground startup verified at `http://localhost:5173/`; tool timeout stopped the foreground server.
- Background hidden dev-server launch did not stay bound in this Codex shell; foreground commands are verified and should be used manually for the live demo.

## Decisions
- Kept the current sup'work product direction and Google Meet path; did not revive TalentFlow IQ's Teams/Graph-first scheduling path.
- Restored TalentFlow IQ's useful live-demo ideas as sup'work contracts: backend-generated fields, provider/data-source metadata, trace IDs, human approvals, and shared workflow state.
- Kept the frontend page structure intact and adapted backend views into existing UI types to avoid a broad visual rewrite.
- Preserved fixture fallback when the backend is unavailable so prototype work can continue offline.

## Handoff / Next
- Run backend: `python -m uvicorn supwork_backend.main:app --host 127.0.0.1 --port 8787`.
- Run frontend: `cd frontend; npm run dev`.
- Demo URL: `http://localhost:5173/`.
- Demo accounts: `hr@demo.supwork.local` / `demo`, `interviewee@demo.supwork.local` / `demo`.
- Recommended roleplay path: HR logs in, generates interview plan, approves schedule, marks interview complete; candidate logs in/refetches automatically, submits addendum; HR acknowledges addendum and approves follow-up; candidate sees released feedback state.
- Remaining work: surface the new agent trace details directly in the HR Audit page and add a visible demo reset button.
- Released scope: `supwork_backend/`, `tests/`, `frontend/src/api/`, `frontend/src/context/`, `frontend/src/types/`, touched workflow pages, `nic_notes/dev_logs/2026-06-27.md`
